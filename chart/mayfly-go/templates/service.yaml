apiVersion: v1
kind: Service
metadata:
  name: {{ include "mayfly-go.fullname" . }}
  labels:
    {{- include "mayfly-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mayfly-go.selectorLabels" . | nindent 4 }}
